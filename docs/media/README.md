# Media Capture

Use `tools/capture_readme_media.sh` on a machine where Chromium headless can initialize WebGL. The current grading package includes the capture script and direct phase URLs:

- `index.html?scene=boss&phase=1`
- `index.html?scene=boss&phase=2`
- `index.html?scene=boss&phase=3`

The ChatGPT sandbox could not capture real WebGL screenshots because Chromium headless failed EGL/ANGLE initialization, so no fake gameplay screenshots are included here.
